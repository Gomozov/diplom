<h1>Устройство № <%= h @device.device_code %> </h1>

<%= GMap.header %>
<%= @map.to_html %>
<%= @map.div(:width => 700, :height => 500) %>

<table>
  <tr>
    <td> ID_записи   </td>
    <td> ID_репорта  </td>
    <td> Параметр   </td>
    <td> Значение   </td>
    <td> Дата создания   </td>
  </tr> 
<% @report.each do |report| %>
  <tr>
    <td><%= h report.id %>   </td>
    <td><%= h report.report_id %>   </td>
    <td><%= h report.key %>   </td>
    <td><%= h report.value %>   </td>
    <td><%= h report.created_at %>   </td>
  </tr>
<% end %>
</table>

<%= link_to 'Back', devices_path %>
